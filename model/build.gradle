apply plugin: "java"
apply plugin: "groovy"

dependencies {
    compile project(":openremote:model")
}

task installDist(type: Copy) {
    from jar.outputs
    into "${project(':deployment').buildDir}/image/manager/extensions"
}
