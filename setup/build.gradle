apply plugin: "java-library"

dependencies {
    api project(":openremote:manager")
    api project(":agent")
    api project(":model")
    api project(":manager")
}

task installDist {
    dependsOn jar
}
